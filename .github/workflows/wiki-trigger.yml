name: Wiki trigger

on:
  workflow_dispatch:
  gollum:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Gollam says
        if: github.event_name == 'gollam'
        run: echo My precious

      - name: Debug
        run: echo $JSON
        env:
          JSON: ${{ toJSON(github.event) }}
